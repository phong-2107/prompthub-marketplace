# 🎯 VISUAL GUIDE - Hướng dẫn trực quan

```
╔══════════════════════════════════════════════════════════════╗
║                    PROMPTHUB PROJECT                         ║
║              Digital Marketplace Platform                    ║
╚══════════════════════════════════════════════════════════════╝
```

---

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                         USER BROWSER                        │
│                    http://localhost:8000                    │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                      NGINX (Port 80)                        │
│              Reverse Proxy & Static Files                   │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                   DJANGO WEB (Port 8000)                    │
│                                                              │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Templates   │  │    Views     │  │   Models     │      │
│  │ (HTML)      │◄─┤  (Logic)     │◄─┤  (Database)  │      │
│  └─────────────┘  └──────────────┘  └──────┬───────┘      │
│                                             │               │
└─────────────────────────────────────────────┼───────────────┘
                     │                        │
         ┌───────────┴──────────┐            │
         ▼                       ▼            ▼
┌──────────────────┐    ┌────────────────────────────┐
│  CELERY WORKER   │    │   POSTGRESQL DATABASE      │
│  (Background     │    │   - Users                  │
│   Tasks)         │    │   - Prompts                │
└────────┬─────────┘    │   - Categories             │
         │              │   - Orders                 │
         ▼              │   - Reviews                │
┌──────────────────┐    └────────────────────────────┘
│   REDIS CACHE    │
│  - Session       │
│  - Celery Queue  │
└──────────────────┘
```

---

## 🔄 Request Flow

```
1. User visits http://localhost:8000/
         │
         ▼
2. Nginx forwards to Django
         │
         ▼
3. Django URLs routing
         │  config/urls.py: '' → apps.core.urls
         │
         ▼
4. Core App URLs
         │  apps/core/urls.py: '' → views.home
         │
         ▼
5. Home View
         │  apps/core/views.py: home()
         │  - Queries database
         │  - Prepares context
         │
         ▼
6. Render Template
         │  templates/marketplace/home.html
         │  - Extends base.html
         │  - Includes sections
         │
         ▼
7. Response to User
         │  HTML + CSS + JS
         │
         ▼
8. Browser displays Homepage
```

---

## 📁 Directory Flow

```
PromptProject/
│
├─┬ apps/                      ← Django Applications
│ ├── core/                    ← Homepage & Core Features
│ │   ├── views.py            ← 👈 home() function
│ │   ├── urls.py             ← Routing: '' → home
│ │   └── models.py
│ │
│ ├── prompthub/              ← Marketplace Models
│ │   ├── models.py           ← Prompt, Category, Review
│ │   ├── views.py
│ │   └── urls.py
│ │
│ └── users/                  ← User Management
│     └── models.py           ← Custom User Model
│
├─┬ templates/                ← HTML Templates
│ └── marketplace/            ← 👈 Active Homepage
│     ├── base.html           ← Master template
│     ├── home.html           ← 👈 Homepage (13 sections)
│     ├── partials/           ← Reusable components
│     │   ├── header.html     ← Navigation
│     │   ├── footer.html     ← Footer
│     │   └── ...
│     └── sections/           ← Homepage sections
│         ├── banner.html     ← Hero section
│         ├── popular_items.html ← Products
│         └── ...
│
├─┬ assets/                   ← 👈 Static Files
│ ├── css/                    ← Stylesheets
│ ├── js/                     ← JavaScript
│ ├── images/                 ← Images
│ └── fonts/                  ← Fonts
│
├─┬ config/                   ← Django Settings
│ ├── settings/
│ │   ├── base.py            ← 👈 Main settings
│ │   ├── development.py
│ │   └── production.py
│ └── urls.py                ← 👈 Root routing
│
└─┬ docker/                   ← Docker Config
  ├── entrypoint.sh
  └── nginx/
      └── nginx.conf
```

---

## 🎨 Template Hierarchy

```
┌────────────────────────────────────────────────────┐
│              base.html (Master)                    │
│  ┌──────────────────────────────────────────────┐ │
│  │  {% load static %}                           │ │
│  │  <html>                                      │ │
│  │    <head>                                    │ │
│  │      CSS includes                            │ │
│  │      {% block extra_css %}                   │ │
│  │    </head>                                   │ │
│  │    <body>                                    │ │
│  │      {% include 'partials/header.html' %}   │ │
│  │                                              │ │
│  │      {% block content %}                    │ │◄─┐
│  │         [Page content here]                 │ │  │
│  │      {% endblock %}                         │ │  │
│  │                                              │ │  │
│  │      {% include 'partials/footer.html' %}   │ │  │
│  │      {% block extra_js %}                   │ │  │
│  │    </body>                                   │ │  │
│  │  </html>                                     │ │  │
│  └──────────────────────────────────────────────┘ │  │
└────────────────────────────────────────────────────┘  │
                                                         │
                                                    Extends
                                                         │
┌────────────────────────────────────────────────────────┘
│
│  ┌────────────────────────────────────────────────┐
└─►│         home.html (Homepage)                   │
   │  ┌──────────────────────────────────────────┐ │
   │  │  {% extends 'marketplace/base.html' %}   │ │
   │  │                                          │ │
   │  │  {% block content %}                    │ │
   │  │                                          │ │
   │  │    {% include 'sections/banner.html' %} │ │◄─── Hero section
   │  │    {% include 'sections/brand.html' %}  │ │◄─── Brand logos
   │  │    {% include 'sections/popular_items.html' %}◄── Products
   │  │    {% include 'sections/selling_products.html' %}
   │  │    {% include 'sections/service.html' %}│ │◄─── Features
   │  │    ... (8 more sections)                │ │
   │  │                                          │ │
   │  │  {% endblock %}                         │ │
   │  └──────────────────────────────────────────┘ │
   └────────────────────────────────────────────────┘
```

---

## 🗄️ Database Schema

```
┌─────────────────────┐
│       User          │
├─────────────────────┤
│ id (PK)            │
│ username           │
│ email              │
│ password           │
│ is_seller          │
│ avatar             │
└──────┬──────────────┘
       │
       │ Creates
       ▼
┌─────────────────────┐        ┌─────────────────────┐
│      Prompt         │────┬───│     Category        │
├─────────────────────┤    │   ├─────────────────────┤
│ id (PK)            │    │   │ id (PK)            │
│ title              │    │   │ name               │
│ description        │    │   │ slug               │
│ thumbnail          │    │   │ icon               │
│ price              │    │   └─────────────────────┘
│ category_id (FK)   │────┘
│ seller_id (FK)     │─────────────┐
│ is_active          │             │
│ views              │             │
│ downloads          │             │
└──────┬──────────────┘             │
       │                            │
       │ Has many                   │ Belongs to
       ▼                            │
┌─────────────────────┐             │
│      Review         │             │
├─────────────────────┤             │
│ id (PK)            │             │
│ prompt_id (FK)     │────┐        │
│ user_id (FK)       │    │        │
│ rating             │    │        │
│ comment            │    │        │
└─────────────────────┘    │        │
                           │        │
       ┌───────────────────┘        │
       │                            │
       ▼                            │
┌─────────────────────┐             │
│       Order         │             │
├─────────────────────┤             │
│ id (PK)            │             │
│ user_id (FK)       │─────────────┘
│ prompt_id (FK)     │
│ amount             │
│ status             │
│ created_at         │
└─────────────────────┘
```

---

## 🚀 Deployment Flow

```
Development (Local)
     │
     │ git push
     ▼
┌────────────────┐
│   Git Repo     │
│   (GitHub)     │
└────────┬───────┘
         │
         │ webhook / CI/CD
         ▼
┌────────────────────────┐
│   CI/CD Pipeline       │
│   - Run tests          │
│   - Build Docker image │
│   - Push to registry   │
└────────┬───────────────┘
         │
         │ deploy
         ▼
┌────────────────────────────────┐
│   Production Server            │
│   (AWS / DigitalOcean)         │
│                                │
│   ┌──────────────────────┐    │
│   │   Docker Containers  │    │
│   │   - Web (Django)     │    │
│   │   - DB (PostgreSQL)  │    │
│   │   - Cache (Redis)    │    │
│   │   - Nginx            │    │
│   │   - Celery           │    │
│   └──────────────────────┘    │
└────────────────────────────────┘
         │
         │ https://
         ▼
┌────────────────┐
│  End Users     │
└────────────────┘
```

---

## 📊 Data Flow

```
User Action → Frontend → Backend → Database → Cache → Response

Example: View Product Page

1. User clicks product
        │
        ▼
2. Browser: GET /product/123/
        │
        ▼
3. Django URL: /product/<id>/
        │
        ▼
4. View: product_detail(request, id)
        │
        ▼
5. Check Redis Cache
        │
   ┌────┴────┐
   │         │
   ▼         ▼
 Found    Not Found
   │         │
   │         ▼
   │    6. Query PostgreSQL
   │         │  SELECT * FROM prompt WHERE id=123
   │         │
   │         ▼
   │    7. Save to Redis Cache
   │         │
   └────┬────┘
        │
        ▼
8. Render Template
        │  product_detail.html
        │  - Product info
        │  - Reviews
        │  - Related products
        │
        ▼
9. Return HTML
        │
        ▼
10. Browser displays page
```

---

## 🔐 Authentication Flow

```
Registration:
User fills form → Validate data → Create user → Send verification email
                                       ↓
                              Save to database
                                       ↓
                              Hash password (bcrypt)

Login:
User enters credentials → Check database → Verify password → Create session
                              ↓                  ↓
                        User exists?      Password matches?
                              ↓                  ↓
                           Yes/No             Yes/No
                              ↓                  ↓
                         (Success)          (Success)
                              ↓
                     Set session cookie
                              ↓
                        Redirect to dashboard
```

---

## 🛒 Shopping Cart Flow

```
┌───────────────────────────────────────────────────────────┐
│  1. Browse Products → 2. Add to Cart → 3. View Cart       │
│                              ↓              ↓              │
│                      Save to session   Display items      │
│                                             ↓              │
│  8. Order Complete ← 7. Process Payment ← 4. Checkout     │
│           ↓                                 ↓              │
│  Update database                    5. Enter details      │
│  Send confirmation                          ↓              │
│  Download access                    6. Payment gateway    │
└───────────────────────────────────────────────────────────┘
```

---

## 📈 Performance Optimization

```
Layer 1: CDN (CloudFlare)
    ↓ (Static files: CSS, JS, Images)
    
Layer 2: Nginx (Reverse Proxy)
    ↓ (Caching, Compression, Load balancing)
    
Layer 3: Redis Cache
    ↓ (Session, Query results)
    
Layer 4: Django (Application)
    ↓ (Business logic, Templates)
    
Layer 5: PostgreSQL (Database)
    ↓ (Persistent data)
    
Layer 6: File Storage (AWS S3)
    ↓ (Media files: avatars, products)
```

---

## 🔧 Development Workflow

```
┌─────────────────────────────────────────────────────────┐
│                                                          │
│  1. Start Docker           docker-compose up -d         │
│                                                          │
│  2. Create Feature Branch  git checkout -b feature-xyz  │
│                                                          │
│  3. Code Changes           Edit files                   │
│                                                          │
│  4. Test Locally           http://localhost:8000        │
│                                                          │
│  5. Run Tests              python manage.py test        │
│                                                          │
│  6. Commit Changes         git commit -m "..."          │
│                                                          │
│  7. Push to Remote         git push origin feature-xyz  │
│                                                          │
│  8. Create Pull Request    GitHub PR                    │
│                                                          │
│  9. Code Review            Team review                  │
│                                                          │
│  10. Merge to Main         Merge PR                     │
│                                                          │
│  11. Deploy                Auto-deploy to production    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 📱 Responsive Design

```
Mobile (< 768px)
┌──────────────┐
│   Header     │
│  ☰ Logo  🌙  │
├──────────────┤
│              │
│   Hero       │
│   Banner     │
│              │
├──────────────┤
│   Product 1  │
├──────────────┤
│   Product 2  │
├──────────────┤
│   Product 3  │
├──────────────┤
│   Footer     │
└──────────────┘

Tablet (768px - 1024px)
┌─────────────────────────┐
│  Header                 │
│  Logo  Menu  Cart  🌙   │
├─────────────────────────┤
│                         │
│     Hero Banner         │
│                         │
├───────────┬─────────────┤
│ Product 1 │ Product 2   │
├───────────┼─────────────┤
│ Product 3 │ Product 4   │
├───────────┴─────────────┤
│       Footer            │
└─────────────────────────┘

Desktop (> 1024px)
┌────────────────────────────────────────────┐
│  Header                                    │
│  Logo    Menu Items    Cart  User  🌙      │
├────────────────────────────────────────────┤
│                                            │
│          Hero Banner with Search           │
│                                            │
├───────────┬──────────┬──────────┬─────────┤
│ Product 1 │Product 2 │Product 3 │Product 4│
├───────────┼──────────┼──────────┼─────────┤
│ Product 5 │Product 6 │Product 7 │Product 8│
├───────────┴──────────┴──────────┴─────────┤
│                 Footer                     │
└────────────────────────────────────────────┘
```

---

**For more details, see:**
- [QUICKSTART.md](QUICKSTART.md) - Complete setup guide
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Directory structure
- [ROADMAP.md](ROADMAP.md) - Development roadmap
